source "https://rubygems.org"

# Match the GitHub Pages legacy build exactly (Jekyll 3.10 + whitelisted
# plugins). Pages ignores this file when building; it exists so any local
# build uses the same stack as production.
gem "github-pages", group: :jekyll_plugins
